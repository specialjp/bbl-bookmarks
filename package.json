{
  "name": "bbl-bookmarks",
  "private": true,
  "description": "Root convenience scripts only — /backend and /frontend are independent packages (ADR-001)",
  "engines": {
    "node": ">=22.22.0"
  },
  "scripts": {
    "dev": "concurrently -n be,fe -c blue,green \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "test": "npm test --prefix backend && npm test --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
