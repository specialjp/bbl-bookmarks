import { createRoot } from 'react-dom/client';

// Placeholder entry — replaced by the full provider composition in the
// auth-wiring commit.
createRoot(document.getElementById('root')!).render(<p>Bookmarks app</p>);
