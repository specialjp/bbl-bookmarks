# api-contract-check

Read the file `.agent/api-contract-check.md` at the repo root and follow its instructions exactly. It is the canonical definition of this command (single source of truth — do not duplicate its logic here).
